# Git Initialization

- git init
- git remote add origin URL
- git add .
- git commit -m "Commit name"
- git branch master -mv main
- git push origin main

# Git push

- git add .
- git commit -m "commit name"
- git push origin main